让容器化应用的部署简单高效

功能
* 数据卷 pod（最小k8s单元）
* 应用健康检查
* 复制应用程序实例
* 弹性伸缩 cpu
* 服务发现 环境变量 dns-》访问地址
* 负载均衡
* 滚等更新
* 服务器编排 yaml？
* 资源监控
* 认证 rbac

Pod 最小部署单元 其中容器共享网络存储 运行在一台主机上
Service 应用服务抽象 定义pod逻辑集合和访问策略
    一个访问入口 分配群集IP地址 来自这个IP的请求将负载均衡转发给后端pod中的应用
    通过lable selection选择一组pod提供服务
volume 
namespace 
label

ReplicaSet
Deployment  管理ReplicaSet
StatefulSet 持久性应用程序
DeaminSet   确保节点运行同一pod
Job 定时执行、一次执行任务

master组件
    * kube-apiserver    集群入口
    * kub-controller-manager    控制器
    * kube0scheduler    为新的pod选择node节点
node组件
    * kubelet
    * kube-proxy
    * docker
    * flannel 容器间通信
    * etcd  k8s通信

关闭 selinux
192.168.10.110
192.168.10.141
192.168.10.145

